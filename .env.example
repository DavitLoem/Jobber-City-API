APP_ENV=

JWT_SECRET_KEY=
JWT_ALGORITHM=

# MongoDB Settings
MONGO_URL=
MONGO_DB_NAME=

# Brevo Email Settings
MAIL_MAILER=smtp
MAIL_HOST=smtp-relay.brevo.com
MAIL_PORT=587
MAIL_USERNAME=a1442c001@smtp-brevo.com
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls

MAIL_FROM_ADDRESS="yTbWt@example.com"
MAIL_FROM_NAME=""
BREVO_API_KEY=


GOOGLE_CLIENT_ID=

MAX_LOGIN_ATTEMPTS=5
LOCKOUT_MINUTES=15
REQUIRE_ADMIN_OTP=False

# Firebase Cloud Messaging (Push Notifications for Chat)
# Provide EITHER one of these two (JSON takes priority if both are set):
# 1. Paste the full service-account JSON contents as a single-line env var (recommended for hosting platforms)
FIREBASE_CREDENTIALS_JSON=
# 2. OR point to a local serviceAccountKey.json file (recommended for local dev)
FIREBASE_CREDENTIALS_PATH=